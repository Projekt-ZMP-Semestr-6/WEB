export interface LoginFormProps {
  email: string;
  name: string;
  password: string;
  password_confirmation: string;
}

export interface InputData {
  name: string;
  label: string;
  type?: string;
}
